// Mode.h

#ifndef MODE_H

#define MODE_H

enum class Mode {
  Invalid,
  Edit,
  PlaceTrace,
  PlaceComponent,
  PlaceHole,
  PlacePad,
  PlaceText,
  PlaceArc,
  PlacePlane,
  PickupTrace,
};

#endif
